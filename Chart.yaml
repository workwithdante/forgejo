apiVersion: v2
name: forgejo-sierra-group
description: Wrapper chart for Forgejo
type: application
version: 0.1.0

dependencies:
  - name: forgejo
    version: "1.0.0"  # Especifica la versión del chart de Forgejo que deseas usar
    repository: "https://artifacthub.io/packages/helm/forgejo-helm/forgejo"  # URL del repositorio de Forgejo
